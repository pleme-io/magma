[package]
name        = "magma-provider-api"
description = "magma — the provider CONTRACT: the `Provider` trait every provider implements (gRPC/tfplugin today, native Rust next) plus the protocol data model it speaks. Depends on magma-cty only, so a native provider needs no gRPC stack."
version.workspace      = true
edition.workspace      = true
rust-version.workspace = true
license.workspace      = true
homepage.workspace     = true
repository.workspace   = true
authors.workspace      = true

[lints]
workspace = true

[dependencies]
magma-cty   = { workspace = true }
async-trait = { workspace = true }
thiserror   = { workspace = true }

[dev-dependencies]
tokio      = { workspace = true }
serde_json = { workspace = true }
